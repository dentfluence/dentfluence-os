*.log
.DS_Store
.env
.env.backup
.env.production
.env.docker-local
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
"backups/"

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Business docs & non-app files (kept locally, never pushed)
/Documents/
/.archive/
/under_review/

# Database dumps — must never reach GitHub (patient data)
*.sql
*.sql.gz
_to_delete/ 

# Laragon writes a MySQL data directory here whenever its Data Directory
# preference points at the project folder (it did on 3-4 Sep, 191 MB of a
# stray database engine landed in the repo root). Never commit it.
/mysql-8.4/
/origin)
