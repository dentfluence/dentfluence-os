{{--
    Coming Soon placeholder — used for tabs not yet built
    Props: $icon, $label, $session
--}}

<div class="coming-soon">
    <i class="ti {{ $icon }}"></i>
    <p class="cs-label">{{ $label }}</p>
    <p class="cs-sub">Session {{ $session }} — coming soon</p>
</div>
