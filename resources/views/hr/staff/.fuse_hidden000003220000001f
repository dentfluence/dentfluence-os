@extends('layouts.app')
@section('page-title', $user->name . ' — HR Profile')

@section('content')
<div class="p-6 space-y-6 max-w-5xl">

    {{-- Back + Actions --}}
    <div class="flex items-center justify-between">
        <a href="{{ route('hr.staff.index') }}" class="text-sm text-gray-500 hover:text-gray-700 flex items-center gap-1">
            <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><polyline points="15 18 9 12 15 6"/></svg>
            Staff Directory
        </a>
        <div class="flex gap-2">
            <a href="{{ route('hr.staff.edit', $user) }}"
               class="px-4 py-2 border border-gray-200 rounded-lg text-sm text-gray-700 hover:bg-gray-50 transition">Edit Profile</a>
            <form method="POST" action="{{ route('hr.staff.destroy', $user) }}"
                  onsubmit="return confirm('Deactivate {{ $user->name }}? They will lose system access.')">
                @csrf @method('DELETE')
                <button type="submit" class="px-4 py-2 border border-red-200 text-red-600 text-sm rounded-lg hover:bg-red-50 transition">
                    Deactivate
                </button>
            </form>
        </div>
    </div>

    @if(session('success'))
    <div class="bg-green-50 border border-green-200 text-green-800 text-sm rounded-lg px-4 py-3">
        {{ session('success') }}
    </div>
    @endif

    {{-- Profile Header --}}
    <div class="bg-white rounded-xl border border-gray-100 shadow-sm p-6">
        <div class="flex items-start gap-5">
            <div class="w-16 h-16 rounded-2xl flex items-center justify-center text-2xl font-bold text-purple-700 bg-purple-100 flex-shrink-0"
                 style="{{ $user->color ? 'background:' . $user->color . '22;color:' . $user->color : '' }}">
                {{ $user->initials }}
            </div>
            <div class="flex-1">
                <div class="flex items-center gap-3 flex-wrap">
                    <h1 class="text-2xl font-display font-semibold text-gray-900">{{ $user->name }}</h1>
                    <span class="text-xs px-2 py-0.5 rounded-full bg-purple-100 text-purple-700 font-medium">{{ $user->role_label }}</span>
                    @if(!$user->is_active)
                    <span class="text-xs px-2 py-0.5 rounded-full bg-red-100 text-red-700 font-medium">Inactive</span>
                    @endif
                </div>
                <p class="text-gray-500 mt-0.5">{{ $user->designation ?? '—' }}</p>
                <div class="flex flex-wrap gap-4 mt-3 text-sm text-gray-600">
                    <span>{{ $user->email }}</span>
                    @if($user->phone)<span>{{ $user->phone }}</span>@endif
                    @if($user->hrProfile?->department)
                    <span class="text-purple-600">{{ $user->hrProfile->department->name }}</span>
                    @endif
                </div>
            </div>
            <div class="text-right text-sm hidden md:block">
                @if($user->hrProfile?->employee_code)
                <p class="text-gray-400 text-xs">Employee Code</p>
                <p class="font-semibold text-gray-800">{{ $user->hrProfile->employee_code }}</p>
                @endif
                @if($user->hrProfile?->years_of_service)
                <p class="text-gray-400 text-xs mt-2">Service</p>
                <p class="font-semibold text-gray-800">{{ $user->hrProfile->years_of_service }}</p>
                @endif
            </div>
        </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">

        {{-- Left: Details --}}
        <div class="lg:col-span-2 space-y-5">

            {{-- HR Details --}}
            <div class="bg-white rounded-xl border border-gray-100 shadow-sm p-6">
                <h2 class="font-semibold text-gray-800 mb-4 pb-3 border-b border-gray-50">HR Details</h2>
                <dl class="grid grid-cols-2 gap-x-6 gap-y-3 text-sm">
                    <div>
                        <dt class="text-gray-400">Joining Date</dt>
                        <dd class="text-gray-800 font-medium">{{ $user->hrProfile?->joining_date?->format('d M Y') ?? '—' }}</dd>
                    </div>
                    <div>
                        <dt class="text-gray-400">Employment Type</dt>
                        <dd class="text-gray-800 font-medium capitalize">{{ str_replace('_', ' ', $user->hrProfile?->employment_type ?? '—') }}</dd>
                    </div>
                    <div>
                        <dt class="text-gray-400">Current Shift</dt>
                        <dd class="text-gray-800 font-medium">
                            @if($user->currentShift?->shift)
                                {{ $user->currentShift->shift->name }}
                                <span class="text-gray-400 font-normal">({{ $user->currentShift->shift->timing }})</span>
                            @else
                                —
                            @endif
                        </dd>
                    </div>
                    <div>
                        <dt class="text-gray-400">Salary</dt>
                        <dd class="text-gray-800 font-medium">
                            @if($user->hrProfile?->basic_salary)
                                ₹{{ number_format($user->hrProfile->basic_salary, 0) }}
                                <span class="text-gray-400 font-normal">/ {{ $user->hrProfile->salary_type == 'hourly' ? 'hr' : 'mo' }}</span>
                            @else
                                —
                            @endif
                        </dd>
                    </div>
                </dl>
            </div>

            {{-- Personal Details --}}
            <div class="bg-white rounded-xl border border-gray-100 shadow-sm p-6">
                <h2 class="font-semibold text-gray-800 mb-4 pb-3 border-b border-gray-50">Personal Details</h2>
                <dl class="grid grid-cols-2 gap-x-6 gap-y-3 text-sm">
                    <div>
                        <dt class="text-gray-400">Date of Birth</dt>
                        <dd class="text-gray-800 font-medium">
                            {{ $user->hrProfile?->date_of_birth?->format('d M Y') ?? '—' }}
                            @if($user->hrProfile?->age) <span class="text-gray-400 font-normal">({{ $user->hrProfile->age }}y)</span> @endif
                        </dd>
                    </div>
                    <div>
                        <dt class="text-gray-400">Gender</dt>
                        <dd class="text-gray-800 font-medium capitalize">{{ $user->hrProfile?->gender ?? '—' }}</dd>
                    </div>
                    <div>
                        <dt class="text-gray-400">Blood Group</dt>
                        <dd class="text-gray-800 font-medium">{{ $user->hrProfile?->blood_group ?? '—' }}</dd>
                    </div>
                    <div>
                        <dt class="text-gray-400">Address</dt>
                        <dd class="text-gray-800 font-medium">{{ $user->hrProfile?->address ?? '—' }}</dd>
                    </div>
                </dl>

                @if($user->hrProfile?->emergency_contact_name)
                <div class="mt-4 pt-4 border-t border-gray-50">
                    <p class="text-xs font-medium text-gray-500 uppercase tracking-wide mb-2">Emergency Contact</p>
                    <p class="text-sm font-medium text-gray-800">{{ $user->hrProfile->emergency_contact_name }}
                        @if($user->hrProfile->emergency_contact_relation)
                        <span class="text-gray-400 font-normal">({{ $user->hrProfile->emergency_contact_relation }})</span>
                        @endif
                    </p>
                    <p class="text-sm text-gray-600">{{ $user->hrProfile->emergency_contact_phone ?? '—' }}</p>
                </div>
                @endif
            </div>

            {{-- Professional / License --}}
            @if($user->role === 'doctor')
            <div class="bg-white rounded-xl border border-gray-100 shadow-sm p-6">
                <h2 class="font-semibold text-gray-800 mb-4 pb-3 border-b border-gray-50">Professional Details</h2>
                <dl class="grid grid-cols-2 gap-x-6 gap-y-3 text-sm">
                    <div>
                        <dt class="text-gray-400">Qualification</dt>
                        <dd class="text-gray-800 font-medium">{{ $user->hrProfile?->qualification ?? '—' }}</dd>
                    </div>
                    <div>
                        <dt class="text-gray-400">Specialization</dt>
                        <dd class="text-gray-800 font-medium">{{ $user->hrProfile?->specialization ?? '—' }}</dd>
                    </div>
                    <div>
                        <dt class="text-gray-400">License No.</dt>
                        <dd class="text-gray-800 font-medium">{{ $user->hrProfile?->license_number ?? '—' }}</dd>
                    </div>
                    <div>
                        <dt class="text-gray-400">License Expiry</dt>
                        <dd class="font-medium">
                            @php $ls = $user->hrProfile?->license_status ?? 'none'; @endphp
                            @if($ls === 'expired')
                                <span class="text-red-600">{{ $user->hrProfile->license_expiry->format('d M Y') }} — Expired</span>
                            @elseif($ls === 'expiring_soon')
                                <span class="text-amber-600">{{ $user->hrProfile->license_expiry->format('d M Y') }} — {{ $user->hrProfile->license_days_remaining }}d left</span>
                            @elseif($ls === 'ok')
                                <span class="text-green-600">{{ $user->hrProfile->license_expiry->format('d M Y') }}</span>
                            @else
                                <span class="text-gray-400">—</span>
                            @endif
                        </dd>
                    </div>
                </dl>
            </div>
            @endif

            {{-- Bank Account Details --}}
            @if($user->hrProfile?->account_number)
            <div class="bg-white rounded-xl border border-gray-100 shadow-sm p-6">
                <div class="flex items-center justify-between mb-4 pb-3 border-b border-gray-50">
                    <h2 class="font-semibold text-gray-800">Bank Account</h2>
                    <span class="text-xs text-gray-400 bg-gray-100 px-2 py-0.5 rounded">Confidential</span>
                </div>
                <dl class="grid grid-cols-2 gap-x-6 gap-y-3 text-sm">
                    <div>
                        <dt class="text-gray-400">Account Holder</dt>
                        <dd class="text-gray-800 font-medium">{{ $user->hrProfile->account_holder_name ?? '—' }}</dd>
                    </div>
                    <div>
                        <dt class="text-gray-400">Bank</dt>
                        <dd class="text-gray-800 font-medium">{{ $user->hrProfile->bank_name ?? '—' }}</dd>
                    </div>
                    <div>
                        <dt class="text-gray-400">Account Number</dt>
                        <dd class="text-gray-800 font-medium font-mono">
                            {{-- Mask middle digits --}}
                            @php
                                $acc = $user->hrProfile->account_number;
                                $masked = strlen($acc) > 4
                                    ? str_repeat('•', strlen($acc) - 4) . substr($acc, -4)
                                    : $acc;
                            @endphp
                            <span id="accNo" data-full="{{ $acc }}" data-masked="{{ $masked }}">{{ $masked }}</span>
                            <button onclick="toggleAccNo()" class="ml-2 text-xs text-purple-600 hover:underline">Show</button>
                        </dd>
                    </div>
                    <div>
                        <dt class="text-gray-400">IFSC Code</dt>
                        <dd class="text-gray-800 font-medium font-mono">{{ $user->hrProfile->ifsc_code ?? '—' }}</dd>
                    </div>
                    @if($user->hrProfile->branch_name)
                    <div class="col-span-2">
                        <dt class="text-gray-400">Branch</dt>
                        <dd class="text-gray-800 font-medium">{{ $user->hrProfile->branch_name }}</dd>
                    </div>
                    @endif
                </dl>
            </div>
            @endif

            {{-- Documents --}}
            <div class="bg-white rounded-xl border border-gray-100 shadow-sm p-6">
                <div class="flex items-center justify-between mb-4 pb-3 border-b border-gray-50">
                    <h2 class="font-semibold text-gray-800">Documents</h2>
                    <a href="{{ route('hr.staff.edit', $user) }}#documents"
                       class="text-xs text-purple-600 hover:underline flex items-center gap-1">
                        <svg class="w-3 h-3" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
                        Upload
                    </a>
                </div>

                @if($user->hrDocuments->isEmpty())
                    <p class="text-sm text-gray-400 text-center py-3">No documents uploaded yet.</p>
                @else
                    <div class="space-y-2">
                        @foreach($user->hrDocuments as $doc)
                        @php
                            $iconColor = match($doc->document_type) {
                                'contract'    => '#7c3aed',
                                'id_proof'    => '#0891b2',
                                'certificate' => '#059669',
                                'bank'        => '#d97706',
                                default       => '#6b7280',
                            };
                        @endphp
                        <div class="flex items-center gap-3 p-3 border border-gray-100 rounded-lg hover:bg-gray-50 group">
                            <div style="width:32px;height:32px;border-radius:7px;background:{{ $iconColor }}18;display:flex;align-items:center;justify-content:center;flex-shrink:0;">
                                <svg width="14" height="14" fill="none" stroke="{{ $iconColor }}" stroke-width="2" viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>
                            </div>
                            <div class="flex-1 min-w-0">
                                <p class="text-sm font-medium text-gray-800 truncate">{{ $doc->label }}</p>
                                <p class="text-xs text-gray-400">{{ $doc->type_label }} · {{ $doc->file_size_human }} · {{ $doc->created_at->format('d M Y') }}</p>
                                @if($doc->notes)<p class="text-xs text-gray-400 italic">{{ $doc->notes }}</p>@endif
                            </div>
                            <a href="{{ Storage::url($doc->file_path) }}" target="_blank"
                               class="text-xs text-purple-600 hover:underline opacity-0 group-hover:opacity-100 transition px-2 py-1 border border-purple-100 rounded-md">
                                View
                            </a>
                        </div>
                        @endforeach
                    </div>
                @endif
            </div>

        </div>

        {{-- Right: Attendance summary + QR --}}
        <div class="space-y-5">

            {{-- Attendance Summary --}}
            <div class="bg-white rounded-xl border border-gray-100 shadow-sm p-5">
                <h3 class="font-semibold text-gray-800 text-sm mb-3">Last 30 Days</h3>
                <div class="grid grid-cols-2 gap-3">
                    <div class="bg-green-50 rounded-lg p-3 text-center">
                        <p class="text-2xl font-bold text-green-600">{{ $presentDays }}</p>
                        <p class="text-xs text-green-700 mt-0.5">Present</p>
                    </div>
                    <div class="bg-red-50 rounded-lg p-3 text-center">
                        <p class="text-2xl font-bold text-red-500">{{ $absentDays }}</p>
                        <p class="text-xs text-red-700 mt-0.5">Absent</p>
                    </div>
                </div>

                {{-- Mini calendar-style attendance dots --}}
                <div class="mt-4 flex flex-wrap gap-1">
                    @foreach($attendance->sortBy('date') as $rec)
                    <div title="{{ $rec->date->format('d M') }}: {{ $rec->status_label }}"
                         class="w-5 h-5 rounded text-center text-xs leading-5 font-medium
                         @if(in_array($rec->status, ['present','late','half_day'])) bg-green-100 text-green-700
                         @elseif($rec->status === 'on_leave') bg-blue-100 text-blue-700
                         @elseif($rec->status === 'holiday') bg-gray-100 text-gray-500
                         @else bg-red-100 text-red-600 @endif">
                        {{ $rec->date->format('d') }}
                    </div>
                    @endforeach
                </div>
            </div>

            {{-- QR Token --}}
            @if($user->hrProfile?->qr_token)
            <div class="bg-white rounded-xl border border-gray-100 shadow-sm p-5 text-center">
                <p class="text-xs font-medium text-gray-500 uppercase tracking-wide mb-3">Attendance QR Code</p>
                <div class="bg-gray-50 rounded-lg p-4 inline-block">
                    {{-- QR placeholder — install simplesoftwareio/simple-qrcode for real QR --}}
                    <div class="w-24 h-24 bg-gray-200 rounded flex items-center justify-center text-xs text-gray-500 mx-auto">
                        QR Code
                    </div>
                </div>
                <p class="text-xs text-gray-400 mt-2 break-all font-mono">{{ Str::limit($user->hrProfile->qr_token, 20) }}</p>
                <p class="text-xs text-gray-400 mt-1">Android app scans this to log attendance</p>
            </div>
            @endif

            {{-- Notes --}}
            @if($user->hrProfile?->notes)
            <div class="bg-white rounded-xl border border-gray-100 shadow-sm p-5">
                <p class="text-xs font-medium text-gray-500 uppercase tracking-wide mb-2">Notes</p>
                <p class="text-sm text-gray-700">{{ $user->hrProfile->notes }}</p>
            </div>
            @endif

        </div>
    </div>
</div>

@push('scripts')
<script>
function toggleAccNo() {
    const el  = document.getElementById('accNo');
    const btn = el.nextElementSibling;
    if (btn.textContent === 'Show') {
        el.textContent = el.dataset.full;
        btn.textContent = 'Hide';
    } else {
        el.textContent = el.dataset.masked;
        btn.textContent = 'Show';
    }
}
</script>
@endpush
@endsection
