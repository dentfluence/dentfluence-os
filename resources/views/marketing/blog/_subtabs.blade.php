{{--
    RETIRED — the Blog Hub is now a single view (the CMS list), so this
    Posts/Calendar sub-tab strip is no longer used. The blog calendar was
    merged into the main Marketing calendar (marketing.calendar). This partial
    is intentionally empty and no longer @included anywhere. Safe to delete
    from disk.
--}}
